generated: '2026-07-18'
method: derived
status: candidate
source: https://docs.trychert.com/llms.txt
note: >-
  No official hosted/remote MCP server was found for Chert as of 2026-07-18 (no npm
  @modelcontextprotocol package, no registry entry, no docs reference). This is a CANDIDATE
  tool list derived from the documented Messaging API operations to seed a future MCP server.
server:
  name: chert
  transport: http
  url: null
tools:
- {name: send_message, description: Simple 1:1 text send, source_operation: 'POST /api/v1/send'}
- {name: send_location, description: One-time location pin on enabled sender lines, source_operation: 'POST /api/v1/locations'}
- {name: create_chat, description: Create a 1:1 chat, source_operation: 'POST /api/v1/chats'}
- {name: create_group, description: Create a group chat (2-31 recipients), source_operation: 'POST /api/v1/chats'}
- {name: send_chat_message, description: Send a message into a chat, source_operation: 'POST /api/v1/chats/{id}/messages'}
- {name: list_messages, description: List messages in a chat, source_operation: 'GET /api/v1/chats/{id}/messages'}
- {name: mark_read, description: Mark a chat read, source_operation: 'POST /api/v1/chats/{id}/mark-read'}
- {name: typing_indicator, description: Send a typing indicator, source_operation: 'POST /api/v1/chats/{id}/typing'}
- {name: react_to_message, description: Add a tapback reaction, source_operation: 'POST /api/v1/messages/{id}/react'}
- {name: list_phone_numbers, description: Sender numbers available to the tenant, source_operation: 'GET /api/v1/phone-numbers'}
- {name: get_attachment_content, description: Fetch inbound attachment bytes, source_operation: 'GET /api/v1/attachments/{attachment_id}/content'}
- {name: create_webhook_subscription, description: Register a webhook endpoint, source_operation: 'POST /api/v1/webhook-subscriptions'}
- {name: list_webhook_subscriptions, description: List webhook endpoints, source_operation: 'GET /api/v1/webhook-subscriptions'}
- {name: replay_event, description: Re-deliver a stored event, source_operation: 'POST /api/v1/events/{id}/replay'}
